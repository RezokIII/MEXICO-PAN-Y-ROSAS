title: La comandancia de la sierra
is-pinned-card: true
tags: advisor
new-page: true
card-image: img/jungleplan.jpg
view-if: via = "armada" and armada_entered = 1

= La comandancia de la sierra

*Consejo de la guerra rural. Only exists while the columns do; when the war ends, so does this desk.*

They sit where Lucio's people sat and Genaro's before them, and they carry the same arithmetic in their heads: how many days of beans, how many trusted mule drivers, which colonel is ambitious and which merely cruel. Nobody elected them and nobody needs to — the sierra promotes by subtraction. What they offer the leadership is the thing no manual sells: the war seen from inside the war.

- @doctrina_dispersion
- @economia_guerra
- @escuela_monte
- @shift_columna
- @root: Return to main

@shift_columna
title: Desplazamiento de emergencia
subtitle: Pull the most-pressured column off the map and set it down on cold ground.
card-image: img/cards/columna_movil.jpg
choose-if: advisor_action_timer == 0 and n_columnas >= 1
unavailable-subtitle: [? if advisor_action_timer > 0 : [+ advisor_action_timer +] months before next advisor action. ?][? if n_columnas < 1 : No column in the field to move. ?]
on-arrival: {!
Q.advisor_action_timer = 6; Q.last_advisor_action = 1;
// find the most-pressured column
var bs = 0, bp = -1;
for (var i = 1; i <= 5; i++) { var z = Q['col'+i+'_zona']; if (z && (Q['presion_'+z]||0) > bp) { bp = Q['presion_'+z]||0; bs = i; } }
if (bs) {
    var oz = Q['col'+bs+'_zona'];
    // pick the coldest war-zone to move to
    var zs = ['guerrero','oaxaca','valle','chihuahua','jalisco']; var dz = zs[0], dp = 999;
    for (var j = 0; j < zs.length; j++) { if (zs[j] !== oz && (Q['presion_'+zs[j]]||0) < dp) { dp = Q['presion_'+zs[j]]||0; dz = zs[j]; } }
    Q['col'+bs+'_zona'] = dz;
    var sh = Math.min(10, Q['pres_'+oz]||0);
    Q['pres_'+oz] = Math.max(0, (Q['pres_'+oz]||0) - sh);
    Q['pres_'+dz] = (Q['pres_'+dz]||0) + Math.round(sh * 0.7);
    Q['presion_'+dz] = Math.max(0, (Q['presion_'+dz]||0) - 30);   // arrives cold — the ring has to be rebuilt
    Q['guar_'+dz] = (Q['guar_'+dz]||0) + 3;                        // but the army follows
    Q.shift_from = oz; Q.shift_to = dz; Q.shift_last = Q.time;
}
Q.parque -= 2; Q.moral_columnas += 2; Q.inteligencia -= 1;
!}

The order goes out in the grammar the sierra uses for its most dangerous moves — a place-name, a date, a route memorized and never written — and the column under the tightest ring simply is not there when the ring closes. It comes down one valley and up another, across country the army maps badly, and sets down on ground where the garrison is thin and the informers are few. It arrives cold: fewer, tired, carrying its wounded, but *alive*, and off the square where it was about to die. The dot moves on the map. The Colonel's careful month of work now points at an empty ridge. This is the whole art of the war — to be, always, one valley over from where the general is certain you are.

- @post_event: Continuar.

@doctrina_dispersion
title: La doctrina de dispersión
subtitle: Rest the hottest zone: the columns scatter, the garrison chases smoke, the ground cools.
card-image: img/jungleplan.jpg
choose-if: advisor_action_timer == 0
unavailable-subtitle: [? if advisor_action_timer > 0 : [+ advisor_action_timer +] months before next advisor action. ?]
on-arrival: {!
Q.advisor_action_timer = 6; Q.last_advisor_action = 1;
// find the hottest garrison and cool it; presence there thins but survives
var zonas = ['guerrero','chihuahua','valle','jalisco','nl','oaxaca','tijuana'];
var hot = zonas[0];
for (var z of zonas) { if (Q['guar_'+z] > Q['guar_'+hot]) { hot = z; } }
Q['guar_'+hot] -= 10;
Q['pres_'+hot] -= 2;
Q.inteligencia += 2;
!}

The order is the oldest one the sierra knows: be water. The column that held the ridge becomes forty people with hoes and grandmothers; the army's informers report, accurately, that there is nothing to report. In a month the garrison's patrol schedule relaxes into the rhythm of men guarding nothing. The zone cools. The war, which lives in the gap between what the state can find and what exists, widens that gap by exactly one province.

@economia_guerra
title: La economía de guerra
subtitle: The zone feeds the war: levies argued not extorted, credit honored, the column as honest customer.
card-image: img/jungleplan.jpg
choose-if: advisor_action_timer == 0
unavailable-subtitle: [? if advisor_action_timer > 0 : [+ advisor_action_timer +] months before next advisor action. ?]
on-arrival: advisor_action_timer = 6; last_advisor_action = 1; funds += 4; sup_peasants += 2; expediente += 2

The comandancia's quiet masterpiece is a ledger: what the coffee brokers skim, published; what the column borrows, repaid at harvest with interest and a receipt. In a country where the state's tax is a bribe and the cacique's tax is a whim, an army that pays its debts is a political program no speech could match. The war chest gains; the villages gain a bank that cannot foreclose; Bucareli gains another report it files under *inexplicable.*

@escuela_monte
title: La escuela del monte
subtitle: The mountain teaches: cadres rotated through the zone come back harder, quieter, better.
card-image: img/jungleplan.jpg
choose-if: advisor_action_timer == 0
unavailable-subtitle: [? if advisor_action_timer > 0 : [+ advisor_action_timer +] months before next advisor action. ?]
on-arrival: advisor_action_timer = 6; last_advisor_action = 1; inteligencia += 4; guerrilla_meter += 1; unity += 2

Three months in the zone is worth three years of study circles: the city cadres come up soft-handed and come down knowing how to read a road, a silence, a stranger's boots. The comandancia runs it like the normal school half of them attended — curriculum, examinations, a graduation with no diplomas and no witnesses. What descends is the war's deepest export: people who cannot be surprised.
